#include "funciones.h"

void inicializacion_juego(unsigned int &m, unsigned int &n, unsigned int *&ptr){
    //reservacion_memoria(ptr, m, n)
    //generacion_inicial_aleatorio(ptr, m, n)

    //detectar_combinaciones(ptr, m, n, combinaciones)
    //caida_fichas(ptr, m, n) -> dentro deberia generar de una vez ficha_aleatoria
    //repetir desde detectar_combinaciones() hasta que no hayan mas
}

void turno(bool &salir_juego, unsigned int m, unsigned int n, unsigned int *&ptr){
    //impresion_tablero_bits()
    //impresion_tablero()
    //entrada_usuario()

    //case opciones disponibles()

    //quitar_columna()
    //agregar_columna()
    //quitar_fila()
    //agregar_columna()
    //quitar_ficha()

    //detectar_combinaciones(ptr, m, n, combinaciones)
    //caida_fichas(ptr, m, n)
    //repetir desde detectar_combinaciones() hasta que no hayan mas
}